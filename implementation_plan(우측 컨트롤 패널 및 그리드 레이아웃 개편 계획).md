# Monitor A: 우측 컨트롤 패널 및 그리드 레이아웃 개편 계획

요청하신 내용에 따라 Monitor A의 UI/UX를 전면 개편하여, 보다 전문적이고 직관적인 관제 화면을 구축하겠습니다.

## 1. 컨트롤 패널 우측 배치 및 UI 고급화
- 기존 하단에 넓게 퍼져 있던 PTZ 제어, 그리드 레이아웃, 프리셋 아이콘들을 **화면 우측 사이드바(Right Sidebar)**로 이동시킵니다.
- 우측 패널은 상하로 꽉 차는(Full Height) 형태로 배치하여 공간 활용도를 높이고, 세로형 레이아웃에 맞춰 아이콘과 컨트롤러 디자인을 훨씬 더 예쁘고 정교하게(고급스러운 VMS 스타일) 다듬겠습니다.

## 2. 분할 화면(그리드 레이아웃) 아이콘 커스텀 제작
- 기존의 단순한 머티리얼 아이콘 대신, **네모 박스 안에 선을 그어 실제 화면 분할을 직관적으로 보여주는 커스텀 아이콘**을 CSS/SVG로 직접 디자인하여 적용합니다. (1분할, 4분할, 9분할, 16분할, 36분할)

## 3. 9분할 및 36분할 그리드 로직 완벽 수정
- 기존에 9분할 클릭 시 레이아웃이 깨지던 버그를 완전히 수정합니다.
- `1x1(1)`, `2x2(4)`, `3x3(9)`, `4x4(16)`, `6x6(36)` 형태로 **정방형(완벽한 바둑판) 그리드**가 자동으로 예쁘게 잡히도록 로직(Math.sqrt 활용)을 뜯어고칩니다.

## 4. 슬롯 플레이스홀더 텍스트 변경
- 화면마다 표시되는 "이 슬롯에 카메라 끌어다 놓기" 텍스트를 **"카메라 끌어다 놓기"**로 간결하게 변경합니다.
- 폰트 스타일을 더 굵고 선명하게 다듬어 가독성을 높이겠습니다.

> [!NOTE]
> 위 내용으로 개편을 진행해도 될까요? **[Proceed]** 버튼을 눌러주시면 즉시 디자인 및 코드 수정 작업에 착수하겠습니다!
