# 화면 및 기획 수정 구현 계획 (반응형 웹 적용 포함)

사용자님의 지적 사항과 엔터프라이즈(VMS) 표준 스펙을 반영한 6가지 핵심 수정 계획입니다.

## 1. 모바일 순찰 뷰 (Mobile Patrol View) 화면 재배치
- 기존 상하 분할(세로로 긴) 구조에서 좌우 분할(가로) 구조로 변경합니다.
- **좌측**: 라이브 영상 화면 (크기를 최적화하여 한눈에 들어오도록 조정)
- **우측**: PTZ 제어 컨트롤러 및 PTT(무전) 통신 아이콘 배치
- 상단 붉은 박스의 짤린 텍스트 영역을 멀티 라인(자동 줄바꿈)이 가능하도록 `break-words` 속성을 추가하여 텍스트가 잘리지 않게 조정합니다.

## 2. 자동 복구 시스템 (Hardware Self-Healing) 레이아웃 꽉 차게 변경
- `main` 태그의 너비 제약을 해제하고 `w-full` 및 `w-full` 100% Flex 레이아웃을 강제하여 화면 좌우 공백 없이 풀사이즈로 꽉 차게 재배치합니다.

## 3. 사이드바(환경 설정) 메뉴 순서 변경
- `BaseLayout.tsx`의 우측 상단 '환경 설정' 드롭다운 메뉴의 정렬 순서를 교체합니다.
- **시스템 설정** 영역을 상단으로 먼저 올리고, **운영 및 제어** 메뉴 영역을 하단으로 내립니다.

## 4. 로그인 및 스플래시 화면 커스텀 로고 적용
- 로그인 뷰(`Login.tsx`, `Signup.tsx` 및 `GuestLayout.tsx`)에 기존 아이콘 대신 기획하신 로고 에셋(`ewvlm_logo_concept_1_1786515721084.png`)을 프로젝트로 복사하여 정중앙과 헤더 등에 적용합니다.

## 5. 회원가입 양식 확장 (이름, 핸드폰 번호 추가)
- 타사 관제 보안솔루션 벤치마킹 기준에 따라 사용자의 이름과 연락처는 승인 심사에 필수적입니다.
- **프론트엔드 (`Signup.tsx`)**: 이름(Name)과 핸드폰 번호(Phone) 입력 폼 필드를 신규 추가합니다.
- **백엔드 (`models.py`, `ewvlm_fastapi_gateway.py`)**: User 데이터베이스 스키마에 `name`과 `phone` 컬럼을 생성하고, 로컬 개발용 `ewvlm.db`를 재초기화하여 API 로직을 업데이트합니다.

## 6. 전역 반응형 웹 및 디스플레이 자동 스케일링 (Responsive Web)
- 현재 일부 고정 크기로 지정되어 해상도에 따라 여백이 남거나 UI가 어긋날 수 있는 부분을 **Flexbox 및 CSS Grid 반응형 유틸리티(`lg:`, `md:`, `w-full`, `h-full`)**로 완전히 개편합니다.
- 사용자의 모니터 해상도(Full HD 1920x1080부터 WQHD, 4K 등)가 달라지더라도 비율에 맞게 자동으로 화면이 꽉 차고 줄어드는(Auto-Scaling) 완벽한 반응형 뷰포트를 제공합니다.

---

> [!IMPORTANT]
> **검토 요청**
> 반응형 스케일링 검토까지 포함된 최종 계획서입니다. **승인해 주시면 즉시 코드 수정을 진행하겠습니다!**
