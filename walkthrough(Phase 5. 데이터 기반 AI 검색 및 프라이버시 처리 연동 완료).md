# Phase 5: 데이터 기반 AI 검색 및 프라이버시 처리 연동 완료

Phase 5의 목표였던 데이터 계층 및 인텔리전스 UI 통합을 모두 마쳤습니다. 이제 단순한 화면 껍데기가 아닌, 실제 백엔드 API와 상호작용하는 지능형 모듈로 동작합니다.

## 1. 자연어 기반 의미 검색 (VSS Semantic Search)
- **AI 쿼리 분석 결과 시각화**: 사용자가 텍스트를 검색할 때, VLM(시각 언어 모델)이 해당 텍스트를 어떻게 분해하여 **검색 의도(Search Intent)**로 변환했는지를 UI에 뱃지(Badge) 형태로 렌더링합니다.
  - 예: "대상: 사람", "행동: 배회", "시간: 최근 24시간" 등의 태그가 실시간으로 노출되어, AI의 사고 과정을 투명하게 확인할 수 있습니다.

## 2. 프라이버시 오토 마스킹 반출 (Privacy Export Workshop)
- **가상 렌더링 파이프라인 연동**: 우측 하단의 **반출 실행** 버튼을 누르면 더 이상 의미 없는 딜레이가 아닌, 백엔드의 `POST /api/v1/video/export/masking` 엔드포인트를 호출하여 작업(비디오 렌더링 및 모자이크 처리)을 지시합니다.
- **다운로드 시뮬레이션**: 암호화(.zip) 체크 여부에 따라 백엔드가 가상의 `.mp4` 또는 `.zip` 파일을 생성하여 내려보내고, 브라우저가 이를 다운로드하는 전체 플로우가 완벽하게 구현되었습니다.

## 3. 실시간 BI 대시보드 (Realtime BI Dashboard)
- **프론트엔드 하드코딩 제거**: 기존에 정해진 수식으로 돌아가던 엉터리 객체 계수(사람, 차량 수)를 전부 걷어냈습니다.
- **데이터베이스 연동**: 백엔드의 `GET /api/v1/bi/stats` 엔드포인트를 신설하여, 현재 시스템에 쌓인 실제 로그 수를 기반으로 실시간 히트맵 존 밀집도와 사람/차량 통계 데이터를 뿌려주도록 재설계했습니다. 이벤트 로그가 발생할 때마다 그래프가 동적으로 바뀔 것입니다.

> [!TIP]
> **테스트 방법**
> 1. **VSS Semantic Search**: "최근 서쪽 주차장에 나타난 수상한 사람"이라고 검색한 뒤, 검색어 인풋 박스 아래에 AI가 추출한 '대상/행동/시간' 배지가 뜨는지 확인하세요.
> 2. **Privacy Export Workshop**: 코덱/워터마크/비밀번호 설정을 변경해 보고, '반출 실행' 버튼을 눌러보세요. 2.5초 후 실제로 브라우저가 파일을 다운로드하는지 확인하세요.
> 3. **Realtime BI Dashboard**: 중앙 화면의 '사람', '차량', '고위험군' 숫자가 새로고침 시마다 백엔드 상태에 맞게 변하는지, 그리고 시간별 경고 추이 그래프가 다이나믹하게 표시되는지 확인하세요.

다음 단계인 **Phase 6 (PTZ 카메라 능동 제어 계층)** 개발을 원하시면 편하게 말씀해 주세요!
