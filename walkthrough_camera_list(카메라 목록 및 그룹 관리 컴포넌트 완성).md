# 카메라 목록 및 그룹 관리 컴포넌트 완성

고객님께서 요청하신 **카메라 목록 및 그룹 관리 전용 뷰어(`CameraListManager.tsx`)** 개발이 완료되었습니다! 
완전히 분리된 화면에서 수십 대의 카메라를 표 형태로 깔끔하게 관리할 수 있습니다.

## 🚀 추가된 주요 기능

### 1. 전역 상태 기반 데이터 관리 (Zustand)
- `useCameraStore.ts`를 도입하여 카메라 데이터와 그룹(폴더) 데이터를 중앙에서 관리하도록 아키텍처를 구성했습니다.
- 여기서 변경된 정보(VLM 켜기/끄기, IP 수정 등)는 다른 모니터 화면에도 실시간으로 동기화됩니다.

### 2. 카메라 목록 조회 및 그룹핑 UI
- **좌측 사이드바:** 카메라 그룹(섹터 1, 섹터 2, 미배정 등) 목록을 표시합니다. 클릭 시 해당 그룹의 카메라만 필터링되어 우측 표에 표시됩니다.
- **우측 데이터 표 (Data Grid):** 선택된 그룹의 카메라 현황을 한눈에 볼 수 있는 표입니다.
- **자체 검색창 내장:** 표 상단에 위치한 돋보기 창에 IP 주소나 카메라 이름을 입력하면 **즉시 검색(실시간 필터링)**됩니다.

### 3. 슬라이드 오버(Slide-over) 빠른 수정 패널
- 표에서 카메라 목록(Row)을 클릭하면, 화면이 다른 곳으로 이동하지 않고 **우측에서 슬라이드 패널**이 스르륵 열립니다.
- 여기서 이름, IP 주소, 해상도, VLM 기능 켜기/끄기 등을 즉시 수정한 뒤 [저장]을 누르면 목록에 바로 반영됩니다.

---

## 🛠 사용 방법
1. 우측 상단의 톱니바퀴(환경 설정) 드롭다운 메뉴를 클릭합니다.
2. 새롭게 추가된 **[카메라 목록 및 그룹 관리]** 버튼을 클릭합니다.
3. 카메라 목록을 둘러보시거나 그룹을 묶고, 검색창을 테스트해 보세요!

> [!TIP]
> 기존의 "카메라 환경설정" 메뉴는 "카메라 신규 등록"으로 이름을 변경하여 역할 분담을 명확히 했습니다.
