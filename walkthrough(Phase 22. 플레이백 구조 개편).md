# Phase 22. 다채널 동기화 플레이백 동적 렌더링 및 캡처 파이프라인 구현 완료

요청하신 "카메라 1대 선택 시 1화면 전환", "좌측 카메라 리스트 선택 연동", "2번 카메라 실시간 캡처 지원" 작업이 모두 성공적으로 완료되었습니다. 

## 1. 프론트엔드 UI/UX 대대적 개편
- **카메라 리스트 연동**: 좌측 사이드바에 실시간 모니터링 화면과 동일한 형태의 **카메라 목록 패널**을 새롭게 추가하였습니다. 상태 관리(Zustand)와 API 통신을 통해 실제 DB에 등록된 카메라 목록을 불러와 렌더링합니다.
- **동적 화면 레이아웃 (Grid)**: 체크박스를 통해 카메라를 선택할 수 있으며, 선택된 카메라 수에 따라 최적의 레이아웃으로 변경됩니다.
  - **1대 선택 시**: `1x1` 단일 화면 모드로 진입하여 화면 가득 크게 표출됩니다.
  - **2대 선택 시**: `2x1` 2분할 화면으로 렌더링됩니다.
  - **3~4대 선택 시**: `2x2` 4분할 화면으로 렌더링됩니다.
- **타임라인 시간축 픽스**: 하단 타임라인의 `CH3/CH4` 트랙과 시간축 라벨이 겹치는 현상을 해결하기 위해 컨테이너의 내부 여백(Padding)을 넉넉하게 확장(`pb-8`)하고 레이아웃을 다듬어 가독성을 확보했습니다.

## 2. 백엔드 동적 라우팅 및 캡처 스크립트 확장
- **캡처 스크립트(`capture_cctv_sample.py`)**: `192.168.0.71(CH1)` 카메라뿐만 아니라 `192.168.0.72(CH2)` 카메라까지 동시에 H.264로 트랜스코딩하여 녹화하도록 다중 프로세싱 스크립트로 개편했습니다. 
- **동적 비디오 스트리밍 API**: `/api/v1/records/{record_id}/stream` 엔드포인트를 업데이트하여 프론트엔드에서 요청하는 `CAM-01`, `CAM-02` ID에 따라 각각 생성된 `record_71.mp4`, `record_72.mp4` 파일로 매핑되어 실시간 부분 스트리밍(Partial Content)을 응답하도록 적용 완료하였습니다.

> [!TIP]
> 이제 브라우저 창에서 **[다채널 플레이백]** 메뉴로 이동하시면, 좌측 카메라 리스트에서 원하는 채널을 클릭하여 1화면부터 4화면까지 즉시 동적으로 레이아웃이 변경되는 것을 직접 테스트해보실 수 있습니다!
