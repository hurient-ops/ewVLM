# Phase 9: 통합 관제 및 실시간 현장 통신 (Incident & Dispatch)

Phase 9 작업이 성공적으로 완료되었습니다. 이번 업데이트를 통해 웹 관제실과 모바일 현장 요원 앱 간의 **실시간 웹소켓(WebSocket) 기반 양방향 통신**이 구축되었습니다.

## 주요 변경 사항

### 1. 통합 알람 센터 (Alert Center Dashboard)
- 새롭게 추가된 **통합 알람 센터**(`/alert-center`) 화면에서 실시간으로 발생하는 위협 알람(침입 감지, 과속 등)을 큐(Queue) 형태로 관리합니다.
- 알람을 선택하고 **'모바일 현장 요원 디스패치'** 버튼을 누르면, 백엔드 서버(`/api/v1/alerts/{alert_id}/dispatch`)를 거쳐 웹소켓으로 메시지가 브로드캐스트됩니다.

### 2. 모바일 순찰 앱 실시간 통신 (Mobile Patrol App)
- `MobilePatrolApp.tsx`가 백엔드 웹소켓(`/ws/alerts`) 서버와 실시간으로 연결됩니다.
- 관제실에서 보낸 '디스패치' 명령을 즉시 수신하여 상단 비상 알림 배너(Emergency Banner)에 동적으로 표시합니다. 
- 수신된 위협 수준(Critical vs Warning)에 따라 배너의 테두리 색상과 펄스(Pulse) 애니메이션이 다르게 렌더링됩니다.

### 3. 네비게이션 연동
- 상단 톱니바퀴(환경 설정) 드롭다운 메뉴의 **'운영 및 제어'** 섹션에 **'통합 알람 센터'** 로 이동하는 단축 링크를 추가했습니다.

## 테스트 방법
1. 웹 브라우저 창 2개를 분리해서 엽니다.
2. 한쪽 창은 **[통합 알람 센터 (http://localhost:5174/alert-center)](http://localhost:5174/alert-center)** 를 엽니다.
3. 다른 창은 **[모바일 현장 뷰 (http://localhost:5174/mobile-patrol)](http://localhost:5174/mobile-patrol)** 를 엽니다.
4. 알람 센터 창에서 특정 알람을 클릭하고 `디스패치` 버튼을 누르면, 거의 동시에 모바일 창 상단에 빨간색 비상 알림 배너가 뜨는 것을 확인할 수 있습니다.
