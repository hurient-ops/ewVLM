# 다채널 동기화 플레이백 동적 카메라 선택 및 레이아웃 개선

사용자의 요청에 따라, 지정된 2대의 CCTV를 모두 실시간으로 H.264 녹화하고, 프론트엔드 좌측에 카메라 리스트를 추가하여 선택된 카메라만 최적의 레이아웃으로 동영상 재생이 되도록 구축합니다.

## 오픈 이슈 및 확인 요청 사항 (Open Questions)
> [!NOTE]
> - 프론트엔드 좌측에 추가할 카메라 리스트는 실시간 모니터링 화면(LiveMonitor)과 유사하게 71번(CH1), 72번(CH2) 카메라 리스트가 표출되며 체크박스를 통해 선택할 수 있도록 구성됩니다.
> - 카메라가 1대만 선택된 경우, 기존 4분할 화면이 아닌 **단일 화면(전체 화면) 레이아웃**으로 변경되어 큼직하게 재생됩니다. (2대가 선택되면 2분할, 등등 동적 렌더링)
> - 해당 기획대로 진행해도 괜찮으실까요?

## 제안된 변경 사항 (Proposed Changes)

---
### 백엔드 (Backend - 스트림 캡처 및 라우팅)

#### [MODIFY] `capture_cctv_sample.py`
- 기존 71번 카메라 단일 녹화 스크립트를 확장하여 `192.168.0.71`과 `192.168.0.72` 2대의 카메라를 모두 FFmpeg로 실시간 캡처합니다.
- 저장 파일명을 각각 `record_71.mp4`, `record_72.mp4`로 저장합니다. (해상도는 profile1 H.264 적용)

#### [MODIFY] `ewvlm_fastapi_gateway.py`
- `/api/v1/records/{record_id}/stream` 엔드포인트를 정적 파일 반환에서 동적 파일 반환으로 수정합니다.
- `record_id`가 `CAM-01`로 들어오면 `record_71.mp4`를, `CAM-02`로 들어오면 `record_72.mp4`를 부분 요청(206 Partial Content)으로 스트리밍하도록 매핑 로직을 추가합니다.

---
### 프론트엔드 (Frontend - UI 구조 개편 및 CSS 픽스)

#### [MODIFY] `MultiChannelSyncPlayback.tsx`
- 화면 최상단 컨테이너를 `flex-row` 구조로 변경하고, **좌측 영역(Left Sidebar)에 카메라 리스트 패널**을 추가합니다.
- Zustand 전역 상태(useCameraStore)를 불러와 카메라 리스트를 렌더링하고 체크박스 다중 선택 로직을 구현합니다.
- **동적 화면 분할(Grid Layout)**: 
  - `selectedCameras.length === 1` 일 경우 `grid-cols-1 grid-rows-1` 적용 (각각 한 화면 렌더링 요청 반영)
  - 선택된 카메라 수에 따라 레이아웃과 `video` 태그 소스 URL을 동적으로 매핑합니다.
- **시간축 UI 버그 수정**: 타임라인 영역의 컨테이너를 좀 더 넓히고 내부 여백(`pb-10` 등)을 추가하여 CH3/CH4와 시간축 라벨이 전혀 겹치지 않도록 맨 아래로 띄워줍니다.

## 검증 계획 (Verification Plan)

### Automated Tests
- 백엔드 캡처 스크립트를 백그라운드 태스크로 실행하여 2대의 카메라가 정상적으로 H.264 파일로 떨어지는지 검증합니다.

### Manual Verification
- 사용자 화면이 자동 새로고침(Hot-Reload)되면 좌측 카메라 리스트에서 카메라를 체크/언체크하여 영상 레이아웃이 1화면, 2화면으로 동적으로 바뀌는지, 재생되는 저장영상이 선택한 카메라의 실제 영상인지 직접 확인합니다.
