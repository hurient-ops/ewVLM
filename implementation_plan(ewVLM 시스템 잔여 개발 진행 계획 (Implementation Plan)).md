# ewVLM 시스템 잔여 개발 진행 계획 (Implementation Plan)

보고서에서 정리한 5가지 핵심 과제를 본격적으로 개발하기 위한 로드맵입니다. 
전체 시스템을 한 번에 구축하기는 어려우므로, 단계별로 접근하는 것을 제안합니다.

## User Review Required

어느 방향으로 먼저 개발을 시작할지 사용자님의 결정이 필요합니다. 아래 제안된 두 가지 옵션 중 선호하시는 방향을 알려주세요.

> [!IMPORTANT]
> **옵션 A: 프론트엔드 기능 고도화 및 인터랙션 완성 (추천)**
> 백엔드를 붙이기 전에, 프론트엔드 상에서 "가짜 데이터(Mock)"를 활용하여 **VLM 시맨틱 챗봇과 대화**하고, **지도에 화재/침입 알람이 번쩍이는 연출** 등을 먼저 100% 완벽하게 구현하여 전체 흐름(User Flow)을 완성하는 방향입니다.
> 
> **옵션 B: 백엔드 서버(API/DB) 기초 공사 시작**
> Node.js(Express) 또는 Python(FastAPI) 등으로 실제 백엔드 서버를 구성하고, 데이터베이스를 연결하여 프론트엔드와 통신하는 뼈대를 먼저 구축하는 방향입니다.

---

## 🛠️ 제안하는 1단계 개발 목표 (옵션 A 선택 시)

만약 프론트엔드 고도화(옵션 A)를 선택하신다면, 다음과 같은 기능들을 먼저 구현할 계획입니다.

### 1. VLM 분석 탭 기능 완성
- **VLM 실시간 이벤트 피드:** 단순한 고정 텍스트가 아니라, 10초마다 랜덤한 AI 감지 이벤트(화재, 침입, 쓰러짐 등)가 동적으로 추가되는 상태 관리(Zustand) 로직 구현.
- **VSS 시맨틱 챗봇 구현:** 사용자가 하단 입력창에 "빨간 옷 입은 사람 찾아줘"라고 입력하면, VLM 어시스턴트가 타이핑 효과와 함께 그럴듯한 답변과 임시 영상을 반환하는 채팅 시스템 구현.
- **보고서 자동 생성 인터랙션:** 이벤트 클릭 시 우측에 사건 보고서가 자동으로 타이핑되며 작성되는 애니메이션 연출.

### 2. GIS 대시보드 동적 통신 (대시보드 탭)
- **이벤트-맵 동기화:** VLM 탭에서 "침입 감지" 이벤트가 발생하면, 대시보드 탭의 카카오맵 특정 마커(예: CAM-04)가 붉은색으로 깜빡이며 경고 팝업이 뜨는 글로벌 상태(Global State) 연동.

### 3. 저장영상(Playback) 타임라인 상호작용
- 마우스 드래그로 타임라인을 움직이면 상단의 모형 영상(이미지) 투명도나 밝기가 변하게 하여 실제로 영상을 탐색하는 듯한 인터랙션 구현.

---

## ❓ Open Questions

1. 위에서 제시한 **옵션 A (프론트엔드 완벽 구현)**와 **옵션 B (백엔드 인프라 구축)** 중 어떤 것을 먼저 진행하고 싶으신가요?
2. 백엔드(옵션 B)를 구성한다면, 주로 사용하시는 언어/프레임워크(예: Node.js, Python, Java Spring)가 따로 있으신가요?
