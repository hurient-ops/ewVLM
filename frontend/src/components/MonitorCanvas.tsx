import React, { useState } from 'react';
import { Play, Pause, AlertTriangle, ShieldAlert, CheckCircle, VideoOff, Layers } from 'lucide-react'; interface MonitorSlot { id: number; cameraId: string | null; cameraName: string | null; status: 'empty' | 'linking' | 'active' | 'alert'; fps: number; detections: string[];
} interface MonitorCanvasProps { selectedCam: string | null; onActionTrigger: (msg: string) => void;
} export default function MonitorCanvas({ selectedCam, onActionTrigger }: MonitorCanvasProps) { const [slots, setSlots] = useState<MonitorSlot[]>([ { id: 1, cameraId: 'CAM-01', cameraName: '외곽 1구역 펜스 북부', status: 'active', fps: 30, detections: ['침입 시도 차단선 인접'] }, { id: 2, cameraId: 'CAM-02', cameraName: '자재 창고 출입구', status: 'active', fps: 29.8, detections: ['지게차 주행 상태'] }, { id: 3, cameraId: null, cameraName: null, status: 'empty', fps: 0, detections: [] }, { id: 4, cameraId: null, cameraName: null, status: 'empty', fps: 0, detections: [] }, ]); const [activeLayout, setActiveLayout] = useState<4 | 1>(4); const handleDragOver = (e: React.DragEvent) => { e.preventDefault(); }; const handleDrop = (e: React.DragEvent, slotId: number) => { e.preventDefault(); const cameraId = e.dataTransfer.getData("cameraId"); if (!cameraId) return; onActionTrigger(`[GSTREAMER] Slot ${slotId} ↔ ${cameraId} 파이프라인 링킹 트리거`); // Update slot to simulation loading setSlots(prev => prev.map(slot => { if (slot.id === slotId) { return { ...slot, cameraId: cameraId, cameraName: cameraId === 'CAM-01' ? '외곽 1구역 펜스 북부' : cameraId === 'CAM-02' ? '자재 창고 출입구' : cameraId === 'CAM-03' ? '중앙 변전실 내부' : cameraId === 'CAM-04' ? '본관 메인 로비' : '하역장 차량 진입로', status: 'linking', fps: 0, detections: ['GStreamer NVMM 버퍼 수집 중...'] }; } return slot; })); // Settle down connection after 1.5s (Simulating FastAPI connection success) setTimeout(() => { setSlots(prev => prev.map(slot => { if (slot.id === slotId) { onActionTrigger(`[SUCCESS] Slot ${slotId} ↔ ${cameraId} GStreamer 파이프라인 링크 정상 수립 완료`); return { ...slot, status: 'active', fps: 30.0, detections: ['정상 관제 구동 완료'] }; } return slot; })); }, 1500); }; const triggerMockAlert = (slotId: number) => { setSlots(prev => prev.map(slot => { if (slot.id === slotId && slot.cameraId) { onActionTrigger(`[ALERT] [위기상황] Slot ${slotId} (${slot.cameraId}) 이상 침입 행위 정황 포착! VLM 3초 세그먼트 분석 개시`); return { ...slot, status: 'alert', detections: ['⚠️ 침입 차단선 돌파 정황 감지!', 'VLM 3초 정밀 판독 시뮬레이션 중...'] }; } return slot; })); // AI Action Complete after 2.5s (Upstage Solar/Ollama model processing) setTimeout(() => { setSlots(prev => prev.map(slot => { if (slot.id === slotId && slot.cameraId) { onActionTrigger(`[SUCCESS] [VLM 판독완료] "작업자 보안 구역 내 안전 미준수 및 침입" (Solar 판독 신뢰도: 96.8%) -> 가상 모바일 패트롤 및 IP Audio TTS 긴급 송출 완료.`); return { ...slot, status: 'alert', detections: ['🚨 위험 등급 판독: 2종 위해 행위 확정', '패트롤 카 메신저 호출 송출 완료'] }; } return slot; })); }, 3000); }; return ( <div className="h-full flex flex-col space-y-4"> {/* 화면 제어 옵션 탑 바 */} <div className="flex items-center justify-between"> <div className="flex items-center space-x-2 bg-[#121724] p-1.5 rounded-lg border border-[#232C3F]"> <button onClick={() => setActiveLayout(4)} className={`px-3 py-1 text-xs font-semibold rounded-md transition ${activeLayout === 4 ? 'bg-[#3B82F6] text-white' : 'text-[#8E9AA8] hover:text-white'}`} > 4분할 화면 </button> <button onClick={() => setActiveLayout(1)} className={`px-3 py-1 text-xs font-semibold rounded-md transition ${activeLayout === 1 ? 'bg-[#3B82F6] text-white' : 'text-[#8E9AA8] hover:text-white'}`} > 전체 단일 화면 </button> </div> <div className="text-xs text-[#8E9AA8] flex items-center space-x-1.5"> <span className="w-2 h-2 rounded-full bg-[#10B981] animate-ping"></span> <span>CCTV RTSP 스트리밍 디코더: </span> <span className="text-white font-mono font-bold">NVIDIA NVDEC Hardware Accelerated</span> </div> </div> {/* 모니터 채널 그리드 */} <div className={`grid gap-4 flex-1 ${activeLayout === 4 ? 'grid-cols-2' : 'grid-cols-1'}`}> {slots.map(slot => { const isSlotActive = slot.status === 'active'; const isSlotLinking = slot.status === 'linking'; const isSlotAlert = slot.status === 'alert'; const isSlotEmpty = slot.status === 'empty'; let borderTheme = 'border-[#232C3F] bg-[#121724]'; if (isSlotLinking) borderTheme = 'border-yellow-500 bg-[#121724] animate-pulse'; if (isSlotAlert) borderTheme = 'border-[#EF4444] bg-[#1A0E1A]'; return ( <div key={slot.id} onDragOver={handleDragOver} onDrop={(e) => handleDrop(e, slot.id)} className={`rounded-xl border p-4 flex flex-col justify-between relative transition duration-300 ${borderTheme}`} > {/* 상단 메타 헤더 */} <div className="flex items-center justify-between"> <div className="flex items-center space-x-2"> <span className="bg-[#070A13] px-2 py-0.5 rounded text-xs font-mono font-bold text-[#3B82F6] border border-[#232C3F]"> CH-0{slot.id} </span> {slot.cameraId && ( <span className="text-sm font-bold text-white"> {slot.cameraName} ({slot.cameraId}) </span> )} </div> {slot.fps > 0 && ( <span className="text-xs font-mono text-[#10B981]"> {slot.fps.toFixed(1)} FPS </span> )} </div> {/* 중앙 비디오 영역 플레이스홀더 (UI 시각 피드백 제공) */} <div className="my-4 flex-1 min-h-[140px] bg-[#070A13] rounded-lg border border-[#232C3F] flex flex-col items-center justify-center relative overflow-hidden"> {isSlotEmpty && ( <div className="flex flex-col items-center space-y-2"> <VideoOff className="w-10 h-10 text-[#232C3F]" /> <span className="text-xs text-[#8E9AA8]">카메라를 드래그해서 드롭하세요</span> </div> )} {isSlotLinking && ( <div className="flex flex-col items-center space-y-2"> <div className="w-8 h-8 border-4 border-yellow-500 border-t-transparent rounded-full animate-spin"></div> <span className="text-xs text-yellow-500 font-mono">FastAPI API 링킹 설정 중...</span> </div> )} {isSlotActive && ( <div className="absolute inset-0 flex flex-col justify-between p-4 bg-gradient-to-t from-black/80 via-transparent to-black/30"> <div className="w-full flex justify-between items-start"> <span className="bg-black/60 px-2 py-0.5 rounded text-[10px] text-green-400 font-bold border border-green-500/40"> ● LIVE STREAM </span> </div> {/* 가상 비디오 파형 시각화 */} <div className="w-full h-8 flex items-end space-x-1 opacity-40"> {[1,2,3,4,3,2,3,4,5,6,5,4,3,2,1,2,3,4,3,2].map((h, i) => ( <div key={i} className="flex-1 bg-[#3B82F6] rounded-t" style={{ height: `${h * 15}%` }}></div> ))} </div> </div> )} {isSlotAlert && ( <div className="absolute inset-0 bg-[#290F0F]/90 flex flex-col items-center justify-center p-4"> <ShieldAlert className="w-12 h-12 text-[#EF4444] animate-bounce" /> <span className="text-sm font-bold text-[#EF4444] mt-2">🚨 VLM 정밀 탐지 이벤트 발동!</span> <span className="text-[11px] text-gray-400 text-center mt-1 leading-relaxed"> Llama 3.2 11B 및 Upstage Solar 비동기 추론 엔진 실시간 추적 중 </span> </div> )} </div> {/* 하단 탐지 정보 바 */} <div className="flex items-center justify-between border-t border-[#232C3F]/50 pt-3"> <div className="text-xs flex items-center space-x-1.5 text-[#8E9AA8] truncate"> <span className="font-bold text-white">AI 상태: </span> {slot.detections.length > 0 ? ( <span className="text-yellow-400 truncate">{slot.detections[0]}</span> ) : ( <span>인공지능 대기 상태</span> )} </div> {slot.cameraId && (isSlotActive || isSlotAlert) && ( <button onClick={() => triggerMockAlert(slot.id)} className="text-[10px] bg-[#EF4444]/20 hover:bg-[#EF4444] text-[#EF4444] hover:text-white transition px-3 py-1.5 rounded font-bold border border-[#EF4444]/40" > 가상 위협 트리거 </button> )} </div> </div> ); })} </div> </div> );
