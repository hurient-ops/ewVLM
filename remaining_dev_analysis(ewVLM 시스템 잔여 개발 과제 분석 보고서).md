# 🔍 ewVLM 시스템 잔여 개발 과제 분석 보고서

현재까지 프론트엔드 UI/UX 기획 및 레이아웃, 화면 라우팅, 모니터(관제) 뷰어의 핵심 기능(카카오맵 스카이뷰, PTZ 드래그앤드롭 컨트롤, 그리드 분할 등)을 성공적으로 구현하였습니다. 

이 시스템이 실제 상용 가능한 **VLM(Video Language Model) 기반 지능형 VMS**로 동작하기 위해 앞으로 남은 주요 개발 과제들을 5가지 카테고리로 분석하여 정리했습니다.

---

## 1. 🎥 실시간 영상 스트리밍 엔진 연동 (Media Server 연동)
현재 `Monitor A`의 영상 슬롯은 가짜(Mock) 이미지로 표시되고 있습니다.

* **스트리밍 프로토콜 구현:** RTSP/ONVIF로 들어오는 카메라 원본 영상을 브라우저에서 재생 가능하도록 WebRTC 또는 HLS로 변환해주는 미디어 서버(예: MediaMTX, Kurento, GStreamer 등) 연동.
* **PTZ 제어 API 연동:** 우측 사이드바에 예쁘게 만들어둔 PTZ 및 줌/초점 방향키를 클릭했을 때, 실제 카메라 렌즈가 움직이도록 ONVIF `AbsoluteMove`, `ContinuousMove` 명령어 API 통신 구현.
* **디코딩 최적화:** 36분할(6x6) 시 브라우저 메모리 부하를 막기 위해 Sub-stream(저해상도 뷰)과 Main-stream(선택 시 고해상도)을 동적으로 스위칭하는 로직.

## 2. 🧠 VLM AI 분석 코어 시스템 통신 (AI Backend 연동)
방금 이름을 변경한 `VLM 분석` 탭의 기능(현재 퍼블리싱 상태)을 실제 인공지능과 연결해야 합니다.

* **실시간 객체 및 이상행동 탐지:** 영상 프레임을 주기적으로 VLM(Vision-Language Model, 예: LLaVA, GPT-4V, 또는 자체 파인튜닝된 로컬 모델)으로 전송하여 결과를 반환받고 `VLM 분석` 화면의 실시간 피드에 뿌려주는 웹소켓 통신.
* **자연어 기반 시맨틱 챗봇 연동:** "넘어진 사람 찾아줘", "파란 옷 입은 용의자 추적해줘" 와 같은 사용자 질의를 VLM이 이해하고 해당 영상 타임스탬프와 요약을 반환하는 챗봇(VSS 시맨틱 챗) API 연동.
* **사건 보고서 자동 생성:** AI가 분석한 타임라인 데이터를 모아 PDF나 결재 포맷으로 렌더링하는 백엔드 리포팅 시스템 연동.

## 3. 🌐 백엔드 및 데이터베이스 (DB / REST API)
현재 Zustand 등을 통한 프론트엔드 상태 관리에 머물러있는 데이터를 영구 저장해야 합니다.

* **카메라 / 자산 관리 DB:** 카카오맵에 띄울 카메라의 위/경도(GPS) 좌표, IP 주소, 인증 정보(ID/PW)를 저장하고 불러오는 CRUD API.
* **사용자 및 권한 관리 (RBAC):** 관리자/일반사용자 구분 및 JWT/OAuth 기반 로그인, 다중 사이트 접근 권한 제어.
* **감사 로그(Audit Log):** 누가 언제 어떤 카메라의 PTZ를 제어했는지, 언제 영상을 다운로드했는지 시스템 로그 저장.

## 4. 🗺️ GIS 맵 이벤트 동적 연동
대시보드의 카카오맵이 단순 뷰어를 넘어 지휘 통제(War Room) 역할을 하도록 고도화가 필요합니다.

* **이벤트 기반 동적 마커:** VLM이 "1구역 화재"를 감지하면, 카카오맵 상의 해당 카메라 마커가 붉게 점멸하고 정보 팝업창이 자동으로 열리는 양방향 이벤트 통신.
* **다중 레이어 표시:** 반경(Radius) 검색, 폴리곤(Polygon)을 그려 해당 구역 안의 카메라만 한 번에 모니터 A로 띄우는 공간 쿼리 연동.

## 5. 📼 NVR 및 저장영상(Playback) 재생 시스템
`저장영상` 탭의 타임라인 기능에 실제 녹화 데이터를 매핑해야 합니다.

* **타임라인 인덱싱 API:** NVR 스토리지에 저장된 영상의 특정 시간대(Timestamp)를 정확히 호출하여 4배속, 8배속 재생, 역방향 재생을 제어하는 로직.
* **VLM 이벤트 북마크 표시:** 타임라인 하단 바에 AI가 이벤트를 감지했던 시간대만 빨간색 점(Bookmark)으로 표시해 빠르게 돌려볼 수 있도록 동기화.

---

> [!TIP]
> 위 과제들 중 프론트엔드 단에서 우선적으로 가짜(Mock) API를 만들어서라도 **"가장 먼저 동작하는 걸 보고 싶은 기능"**이 있다면 말씀해 주세요. (예: VLM 자연어 챗봇 채팅창 기능 고도화, 타임라인 영상 재생 플레이어 로직 등)
