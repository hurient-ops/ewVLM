# Phase 6: 물리적 하드웨어 능동 제어 (Physical PTZ & ONVIF Layer) 연동 완료

이전 Phase들에서 화면상에 기획해두었던 **공간 캘리브레이션 3D 좌표 수학 연동**과 **지능형 PTZ 카메라 제어** 모듈들이 이제 백엔드 API와 완전히 엮여 작동하기 시작했습니다. 

## 1. 공간 기하학 좌표계 보정 (Geometry Calibration Console)
단순한 폼(Form)이 아니라, 사용자가 카메라 화면(오버레이)을 직접 클릭할 수 있는 **인터랙티브 측거의 기능**이 탑재되었습니다.
- 영상 영역을 마우스로 클릭하면, 프론트엔드가 클릭한 픽셀의 `(X, Y)`를 백엔드로 보냅니다.
- 백엔드에 신설된 `POST /api/v1/cameras/{camera_id}/transform` 엔진은 설정된 고도(Altitude), 틸트(Tilt), 초점 거리(Focal Length)를 바탕으로 픽셀 좌표를 역투영(Perspective Transform)하여 **지면 상의 실제 카메라와의 거리(meter)** 를 계산해 응답합니다.
- 클릭한 위치에 핑(Ping) 애니메이션과 함께 실측 거리가 툴팁으로 나타나는 것을 확인할 수 있습니다.

## 2. 지능형 PTZ 수동 제어 & 핸드오버 (PtzTargetHandover)
- 하단의 '수동 제어 (PTZ)' 조그 셔틀 컨트롤러 버튼(상, 하, 좌, 우, IN, OUT)을 누르면 실제로 백엔드의 ONVIF 컨트롤러에 명령이 하달됩니다.
- 성공 여부가 UI에 **상단 팝업(Toast) 알림**으로 시각적 피드백되도록 개선하여 조작성을 한층 높였습니다.

## 3. PTZ 순찰 체인 스케줄러 (PtzPatrolSchedule)
- 상단의 `순찰 시작` / `순찰 중지` 버튼이 실제 API(`controlPtz(PATROL_START)`)에 직접 연결되었습니다. 버튼을 누르면 API 전송이 성공한 경우에만 토글 상태가 변경되게 함으로써 무결성을 확보했습니다.

> [!TIP]
> **테스트 방법**
> 1. 좌측 메뉴에서 **Calibration**으로 이동한 뒤, 넓은 카메라 뷰 영역 중 바닥(Ground)을 여러 군데 클릭해 보세요. 화면 위쪽일수록 거리가 멀어지고, 아래쪽일수록 거리가 가까워지는 것을 확인하실 수 있습니다. 고도를 바꿔가며 클릭 시 거리가 다르게 찍히는지도 확인해 보세요.
> 2. 좌측 메뉴에서 **PTZ Handover**로 이동하여, 아래의 방향키 조이스틱을 눌러 화면 최상단에 토스트 메시지가 반짝 켜지는 것을 확인해 보세요.

이제 물리 하드웨어를 소프트웨어 단에서 마음대로 주무를 수 있게 되었습니다! 다음 단계인 **Phase 7 (DevOps 인프라 상태 모니터링 및 자가 치유)** 로 넘어가길 원하시면 언제든 편하게 말씀해 주세요.
