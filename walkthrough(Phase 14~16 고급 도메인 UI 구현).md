# Phase 14~16 구현 완료 요약 (PTZ 및 오디오 제어 도메인)

요청하신 고급 도메인 화면 전면 개발 계획 중 **Phase 14 ~ 16**에 해당하는 특수 관제 UI 컴포넌트 3종의 구현이 완료되었습니다.

## 주요 구현 사항

### 1. PTZ 핸드오버 콘솔 (`PtzHandoverConsole.tsx`)
- 다중 PTZ 카메라를 활용하여 단일 이동 객체를 연속적으로 추적하고 제어권을 넘겨주는 UI 구현.
- 좌측에 실시간 궤적 맵을 레이아웃하여 활성 타겟과 인접 카메라 위치를 시각화.
- 우측에 락온(Lock-on)된 대상 카메라 영상 피드 모의 연출 및 인계 권한 제어 리스트 제공.

### 2. PTZ 자율 순찰 스케줄러 (`PtzTourScheduler.tsx`)
- 야간/주간 등 시간대별로 PTZ 프리셋 시나리오(Tour)를 설정하고 자동화하는 스케줄러 콘솔.
- 좌측: 등록된 순찰 투어 목록과 시작/정지 상태 토글.
- 우측: 선택된 투어의 시퀀스 타임라인 표시 및 사용 가능한 프리셋 라이브러리 연동 UI.

### 3. IP 오디오 방송 콘솔 (`IpAudioBroadcastConsole.tsx`)
- VMS 인프라 내에 배치된 다중 구역 IP 스피커 대상 TTS(Text-to-Speech) 비상 방송 송출 콘솔.
- 방송을 원하는 다수의 구역을 체크박스/리스트로 선택 가능 (오프라인 기기 비활성화 처리).
- 음성 보이스(남성 지시형, 여성 안내형, 사이렌) 및 사전 정의된 텍스트 템플릿(경고/대피) 선택 기능.
- `API.broadcastAudio` (현재 모의 지연 2초) 연동을 통한 비동기 상태 UI 표시 (Loading Spinner).

> [!NOTE]
> 해당 파일들은 `App.tsx` 또는 사이드바 네비게이션을 통해 접근 가능하도록 기존 라우팅 구조와 호환되게 작성되었습니다. 모의 백엔드 연동을 위한 UI 껍데기 수준에서 나아가 상태(State)와 임시 타임아웃 이벤트 처리가 모두 구현되어 있습니다.

## 다음 진행 예정 (Phase 17 ~ 19)
다음 단계로는 **AI 옵스(AIOps) 및 자율 복구 도메인**에 속하는 아래 컴포넌트들을 이어서 개발할 예정입니다.
1. `AiLoraFineTuningConsole.tsx`: 모델 오탐지 튜닝 콘솔
2. `EdgeAiOrchestrator.tsx`: 엣지 AI 노드 배포 상태 콘솔
3. `SystemSelfHealingShell.tsx`: 오류 로그 및 자가 치유 CLI 콘솔
