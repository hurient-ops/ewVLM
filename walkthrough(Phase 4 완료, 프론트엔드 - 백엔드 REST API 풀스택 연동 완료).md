# Phase 4 완료: 프론트엔드 - 백엔드 REST API 풀스택 연동 완료

이전 단계에서 구축했던 SQLite DB와 FastAPI 백엔드를 바탕으로, **프론트엔드의 주요 UI 컴포넌트들을 실제 백엔드 API와 연동하는 작업(Phase 4)**을 완수했습니다. 

## 1. 🌐 카카오맵 동적 카메라 렌더링 (GIS)
- **`GisSmartMap.tsx`**: 하드코딩되어 있던 카메라 좌표(`CAMERA_POSITIONS`)를 제거하고, 백엔드의 `/api/v1/cameras` 엔드포인트를 호출하도록 변경했습니다.
- 백엔드 구동 시 자동으로 주입된 더미 카메라 정보(이름, 위/경도, IP 등)를 기반으로 지도에 마커가 동적으로 생성되며, 실시간 WebSocket 이벤트 발생 시 해당 카메라에 🚨 경고 애니메이션이 표시됩니다.

## 2. 🔍 VSS (Video Semantic Search) 기록 연동
- **`VssSemanticSearch.tsx`**: 하단의 검색 결과 캐러셀 UI가 이제 백엔드 DB의 실제 기록(`/api/v1/events/history`)을 가져오도록 구현되었습니다.
- 사용자가 확인해야 할 AI 시맨틱 캡션, 이벤트 신뢰도, 카메라 번호, 발생 시간 등이 하드코딩된 더미 데이터가 아닌 **실제 데이터베이스 이력**으로 표시됩니다.

## 3. 🚨 이벤트 검토 센터 (Event Review Center) 연동
- **`EventReviewCenter.tsx`**: 좌측의 '검토 대기열' 리스트 또한 `/api/v1/events/history` API를 통해 백엔드에서 불러옵니다.
- `쓰러짐(collapsed)` 등 심각한 트리거 클래스는 'Critical'로 분류되고 그 외의 이벤트는 'Warning'으로 색상 및 UI가 자동 변환되도록 연동을 완료했습니다.

---

> [!NOTE]
> **답변: 자주 물어보시는 질문들**
> 
> **Q. 화면에 YOLO분석과 VLM 분석은 어떻게 중지시킬수 있어? / 다시 살릴때는?**
> A. 현재 백엔드 단에서 `yolo_fast_loop.py` 및 게이트웨이 백그라운드 태스크가 분석을 수행합니다. 콘솔에서 해당 프로세스를 중지시키거나, 향후 설정(Settings) UI 화면에 'AI Inference Pause' 토글 버튼을 추가하여 WebSocket 신호로 제어하게 할 수 있습니다. 
> 
> **Q. CCTV TCP/IP 연동 및 디코딩 구현은?**
> A. 본 프로젝트는 실무 환경(RTSP 스트림)을 가정하고 아키텍처가 설계되었습니다. 현재는 데모용 비디오 파일(mock)을 `cv2.VideoCapture`로 읽어 H.264/H.265 디코딩(내부적으로 FFmpeg 등 사용)을 수행하고 있으나, 향후 실제 RTSP URL만 DB의 `Camera.ip_address`로 교체하면 OpenCV나 GStreamer 파이프라인을 통해 즉시 실시간 스트림 처리가 가능합니다.
> 
> **Q. 기능이 적용되지 않은 나머지 27개 화면들은 어떻게 진행할 계획이야?**
> A. 핵심 기능(대시보드, GIS, 검색, 이벤트 처리)은 현재 DB/AI 연동이 적용된 상태입니다. 인증(Auth), 시스템 설정, 다채널 레이아웃 등 나머지 화면들은 다음 단계(Phase 5, 6)에서 점진적으로 API를 추가 연결하고 기능을 살려나갈 계획입니다.

---

> [!TIP]
> **다음 개발 진행**
> 남은 과제(Phase 5: 시스템 설정 및 PTZ 제어 기능 활성화 등)로 넘어갈지, 혹은 현재 구현된 REST API 기능들의 작동을 웹 브라우저를 통해 점검해보실지 말씀해주시면 바로 진행하겠습니다!
