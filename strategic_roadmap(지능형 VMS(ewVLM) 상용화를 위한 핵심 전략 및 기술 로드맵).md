# 지능형 VMS(ewVLM) 상용화를 위한 핵심 전략 및 기술 로드맵

개발을 진행하시면서 느끼셨을 답답함과 근본적인 의문점들에 대해 깊이 공감합니다. 현재 우리는 단순한 웹 개발을 넘어 **차세대 AI 영상 분석 시스템의 패러다임**을 설계하고 있기 때문에 이러한 고민은 상용화와 성공적인 비즈니스를 위해 반드시 거쳐야 할 필수적인 과정입니다.

질문하신 7가지 핵심 의문에 대해 기술적, 비즈니스적 관점에서 속 시원하게 정리해 드립니다.

---

## 1. AI 모델 전략 (Moondream vs 대형 모델 vs 자체 경량 모델)

### Moondream의 한계와 대형 모델 적용의 차별성
현재 적용된 **Moondream**은 매우 가볍고 빠르지만, 복잡한 맥락이나 미세한 이벤트를 정확히 판단해야 하는 상용 CCTV 환경에서는 한계가 있습니다. Llama 3.2 Vision, Solar, Paligemma와 같은 고성능 모델을 조합하면 인식률과 맥락 이해도는 비약적으로 상승합니다. 하지만 단순히 "무거운 모델을 여러 개 돌린다"는 것만으로는 타사(대기업)의 클라우드 기반 솔루션과 차별화하기 어렵고, 하드웨어 비용(GPU)이 기하급수적으로 증가합니다.

### 💡 해결책: 지식 증류(Knowledge Distillation)를 통한 자체 경량 모델 개발
대표님이 생각하신 **"여러 모델을 합성하여 가볍고 고성능인 자체 로컬 AI를 만드는 방법"**이 정확히 정답이자 이 솔루션의 강력한 무기가 될 수 있습니다. 
이를 AI 분야에서는 **지식 증류(Knowledge Distillation)**라고 합니다.
- **방식:** 거대한 "교사(Teacher) 모델"(예: Llama 3.2 Vision, GPT-4o)이 영상을 보고 판단한 고품질의 결과(정답)를, 작고 빠른 "학생(Student) 모델"(예: Moondream, Qwen-VL-Chat-Int4)에게 학습시키는 방식입니다.
- **효과:** 학생 모델은 교사 모델의 추론 능력을 모방하게 되어, **작은 파라미터 크기(가벼움)와 빠른 속도를 유지하면서도 대형 모델에 버금가는 성능**을 내게 됩니다. 이로써 값비싼 GPU 서버 없이 일반적인 엣지(Edge) 서버나 로컬 PC에서도 고성능 VLM을 구동할 수 있는 **독보적인 경쟁력**을 갖추게 됩니다.

---

## 2. VLM vs 기존 지능형 AI(YOLO) 비교 및 작동 방식

| 구분 | 기존 지능형 AI (YOLO 기반) | VLM (시각언어모델) 기반 |
| :--- | :--- | :--- |
| **핵심 원리** | 픽셀 패턴을 통한 **객체 탐지** (Object Detection) | 영상과 자연어를 결합한 **맥락 이해** (Semantic Understanding) |
| **이벤트 설정 방식** | 화면에 직접 선/박스(구획, ROI)를 그리고, "이 선을 사람이 넘으면 알람"과 같이 기계적 규칙 설정 | 자연어로 프롬프트 작성. 예: "이 사람이 흉기를 들고 위협적인 행동을 하고 있는가?" |
| **장점** | 연산이 매우 가볍고 빠름 (실시간 30~60fps). 위치 정보(좌표) 제공에 탁월함. | 구획 설정 없이 다양한 이상 행동(싸움, 배회, 쓰러짐 등)을 유연하게 감지. 복잡한 맥락 파악 가능. |
| **단점** | "사람", "자동차" 등 지정된 객체만 찾을 수 있음. 행동의 '의도'나 '맥락'은 전혀 모름. 잦은 오탐(False Positive). | 연산량이 많아 무겁고 느림. 실시간(30fps) 전수 분석이 사실상 불가능함. |

### VLM의 이벤트 조건 부여 및 확인 방식
기존에는 화면에 가상의 선을 그어 '월담'을 잡았다면, VLM은 **시스템(또는 사용자)이 VLM에게 질문(프롬프트)을 던지고 대답을 받는 형태**로 작동합니다.
- **시스템:** (CCTV 이미지를 VLM에 전달하며) *"이 이미지 안에서 헬멧을 쓰지 않은 작업자가 있는지 확인하고, 있다면 '안전모 미착용', 없다면 '정상'으로 답변해."*
- **VLM:** *"안전모 미착용. 화면 중앙의 작업자가 노란색 조끼를 입었으나 머리에 헬멧이 없습니다."*
VLM은 이렇게 자연어로 된 질문을 이해하고 영상을 분석하여 답을 줍니다.

---

## 3. VLM의 느린 속도, 과연 경쟁력이 있을까? (Hybrid 아키텍처)

VLM만으로 수십 대의 CCTV 영상을 1초에 30프레임씩 분석하는 것은 불가능에 가깝습니다. 따라서 **VLM 단독으로는 기존 솔루션을 완벽히 대체할 수 없으며, 기존 기술과의 융합이 필수**입니다.

### 💡 ewVLM의 핵심 경쟁력: Dual-Loop Hybrid 아키텍처
현재 백엔드(fast_loop.py 등)에 설계되어 있는 구조가 바로 이 속도 문제를 해결하기 위한 하이브리드 방식입니다.
1. **Fast Loop (YOLO 등 경량 모델):** 초당 30프레임으로 영상 전체를 빠르게 훑으면서 "사람"이나 "차량" 등 타겟 객체가 등장하는지, 혹은 움직임이 있는지만 빠르게 감지합니다.
2. **Slow Loop (VLM):** Fast Loop에서 무언가 포착되었을 때만(이벤트 발생 시), 해당 프레임 1~2장만 추출하여 VLM에 넘깁니다. VLM은 1~2초가 걸리더라도 해당 장면이 "단순한 지나감"인지 "범죄 현장"인지 깊이 있게 검증합니다.

이 방식을 통해 **"YOLO의 실시간성 + VLM의 정확도 및 맥락 이해력"**이라는 두 마리 토끼를 모두 잡을 수 있으며, 이것이 기존 단순 객체 탐지 솔루션을 뛰어넘는 강력한 차별점입니다.

---

## 4. 학습 데이터 셋 확보 전략

LLaVA-Video-178K는 좋은 뼈대가 되지만, 보안/CCTV 도메인에 특화된 상용 모델을 만들려면 훨씬 많은 '현장 데이터'가 필요합니다.
1. **오픈소스 범죄/이상 행동 데이터셋 활용:** `UCF-Crime`, `XD-Violence`, `SHTech` 등 학계에 공개된 CCTV 이상행동 데이터셋을 수집하여 VLM 프롬프트 형태로 변환합니다.
2. **합성 데이터(Synthetic Data) 생성:** 게임 엔진(Unreal Engine, Unity)이나 비디오 생성 AI를 활용해 24시간 다양한 날씨, 조명, 범죄 상황이 연출된 가상의 CCTV 영상을 무한대로 생성하여 학습합니다.
3. **Active Learning (사용자 참여형 수집):** 솔루션 배포 초기에는 VLM의 판단 결과를 관제사가 "맞음/틀림"으로 피드백할 수 있는 UI를 제공합니다. 관제사가 교정한 오탐 데이터는 고스란히 귀중한 자체 학습 데이터로 쌓여 모델을 지속적으로 고도화시킵니다. (이것이 데이터 플라이휠입니다)

---

## 5. 학습 방법 및 도구 (Unsloth 활용)

최근 로컬 LLM/VLM 파인튜닝의 표준으로 자리 잡은 것이 **Unsloth** 라이브러리입니다. Unsloth는 메모리 사용량을 절반 이하로 줄이고 학습 속도를 2배 이상 높여줍니다.

### 학습 프로세스 가이드
1. **데이터 포맷팅:** 수집한 이미지/영상과 이에 대한 질문(Q) & 정답(A) 쌍을 OpenAI의 JSONL 형식으로 만듭니다.
2. **환경 구성:** Google Colab(A100 GPU 권장)이나 RunPod 같은 클라우드 GPU 환경에 접속합니다.
3. **학습 실행:** Unsloth가 제공하는 [LLaVA 파인튜닝 무료 Colab 노트북](https://github.com/unslothai/unsloth)을 복사하여, 앞서 만든 JSONL 데이터를 업로드하고 실행 버튼을 누릅니다.
4. **포맷 변환 및 배포:** 학습이 완료되면 Unsloth의 기능을 통해 모델을 1파일 형태인 `GGUF` 포맷으로 내보냅니다. 이를 로컬 PC의 Ollama나 LM Studio에 넣으면 끝납니다.

---

## 6. LoRA / QLoRA 란 무엇이고 어떻게 구현하나?

수십억 개의 파라미터를 가진 AI 모델의 뇌(가중치)를 전부 뜯어고치는 것(Full Fine-Tuning)은 엄청난 시간과 슈퍼컴퓨터가 필요합니다.
- **LoRA (Low-Rank Adaptation):** 기존 모델의 거대한 뇌는 그대로 얼려두고(Freeze), **새로운 지식을 담을 아주 작은 메모리 칩(Adapter)만 뇌 겉면에 덧붙여서 학습시키는 기술**입니다. 학습해야 할 파라미터가 1% 미만으로 줄어들어 일반 그래픽카드(RTX 3090, 4090 등)에서도 학습이 가능해집니다.
- **QLoRA:** 기존 모델의 뇌(가중치) 크기 자체를 4-bit 수준으로 압축(Quantization)하여 메모리를 더욱 극단적으로 아끼면서 LoRA 학습을 진행하는 기법입니다.

**구현 방법:** 직접 복잡한 수학 코드를 짤 필요 없이, 앞서 언급한 **Unsloth 프레임워크**나 Hugging Face의 **TRL (SFTTrainer)** 라이브러리를 사용하면 설정값(`use_4bit=True`, `peft_config`) 하나로 QLoRA 학습이 자동 적용됩니다.

---

## 7. 남은 프론트엔드 UI/UX 개발 방향성

현재 총 27~28개 화면 중 핵심 관제(Monitor A/B)와 카메라 설정 등 뼈대는 구현되었으나, 디테일한 비즈니스 로직 화면들이 남아있습니다.
방향을 잃지 않기 위해 다음 순서로 우선순위를 정해 접근하는 것을 권장합니다.

1. **지능형 기능(핵심 차별화 가치) UI 우선 개발:**
   - `EventReviewCenter.tsx` (이벤트 리뷰 및 오탐지 피드백) : 관제사가 VLM이 탐지한 이벤트를 검토하고 정답/오답을 피드백하여 **학습 데이터를 수집(Active Learning)**하는 화면. 솔루션 경쟁력의 핵심입니다.
   - `VssSemanticSearch.tsx` (자연어 영상 검색) : VLM의 가장 큰 장점인 "빨간 옷 입고 가방 멘 사람 찾아줘" 기능을 시각화하는 화면.
   - `NaturalLanguageRuleCopilot.tsx` : 사용자가 자연어로 알람 규칙(SOP)을 설정하면 이를 VLM 프롬프트로 변환해주는 챗봇형 UI.
2. **부가 관제 및 시스템 UI (후순위):**
   - GIS 맵, NVR 스토리지 대시보드, 시스템 감사 로그 등 기존 VMS에도 존재하는 기능들은 컴포넌트를 모듈화하여 순차적으로 개발해 나갑니다.

**결론적으로, 우리는 단순한 영상 뷰어가 아니라 "가벼운 1차 비전 모델(YOLO)이 눈이 되어 실시간으로 감시하고, 자체 경량화된 VLM이 뇌가 되어 판단하며, 관제사의 피드백으로 스스로 발전하는 지능형 시스템"을 시각화하는 데 모든 개발 역량을 집중해야 합니다.**
