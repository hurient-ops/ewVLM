# 잔여 프론트엔드/백엔드 미구현 기능 상세 분석 리포트

현재 Phase 1 ~ Phase 4를 거치며 "실시간 관제, 지능형 룰 코파일럿(VLM), 재난 대응(SOP/방송), 보안 및 감사"라는 굵직한 메인 파이프라인은 완성되었습니다. 하지만 지적하신 대로, 시스템 폴더 내에는 아직 껍데기(UI Shell) 상태이거나 백엔드 로직이 연결되지 않은 컴포넌트들이 다수 남아있습니다.

이를 시스템 계층별로 세밀하게 분석하여 정리했습니다.

## 1. 차세대 AI & VLM 심화 기능 (Core Intelligence)
이 영역은 룰 생성을 넘어, 비전 AI 모델을 직접 튜닝하고 자연어로 과거 영상을 검색하는 등 심화된 AI 기능들입니다.
- **`VssSemanticSearch.tsx` (의미 기반 영상 검색)**: 
  - **상태**: 부분 구현 (UI 존재)
  - **결핍**: "빨간 트럭을 찾아줘"와 같은 텍스트 쿼리를 벡터로 변환하고, Milvus/Qdrant 같은 Vector DB와 연동하여 과거 NVR 메타데이터를 검색하는 백엔드 파이프라인이 부재합니다.
- **`LoraFinetuningConsole.tsx` (엣지 AI 파인튜닝)**: 
  - **상태**: UI 껍데기만 존재
  - **결핍**: 현장 데이터(이미지 크롭)를 모아 백엔드 GPU 서버로 전송하고, 실제 LoRA 모델 학습(Training Job)을 트리거한 뒤 가중치 파일(.safetensors)을 엣지로 배포하는 MLOps 파이프라인이 없습니다.
- **`PromptGatewayDeploy.tsx` (프롬프트 라우팅 관리)**:
  - **상태**: UI 껍데기만 존재
  - **결핍**: VLM 프롬프트의 버전 관리, A/B 테스트, 카메라별 프롬프트 다중 적용을 위한 백엔드 데이터베이스 및 라우터 엔진이 미구현 상태입니다.

## 2. 하드웨어 및 인프라 제어 (Hardware & Infrastructure)
카메라 자체의 물리적 제어와 네트워크, 서버 하드웨어 상태를 관리하는 로우레벨 시스템입니다.
- **`PtzPatrolSchedule.tsx` & `PtzTargetHandover.tsx` (PTZ 카메라 제어)**:
  - **상태**: 로컬 목업 (버튼 클릭 시 `console.log`만 찍힘)
  - **결핍**: 백엔드에서 ONVIF 프로토콜이나 벤더사 API를 직접 호출하여 실제 카메라의 렌즈를 돌리거나(Pan/Tilt) 줌(Zoom)을 조작하는 하드웨어 제어 데몬이 없습니다. 객체 추적(Handover)을 위한 좌표 변환 알고리즘도 필요합니다.
- **`NetworkTopologyMonitor.tsx` (네트워크 및 PoE 모니터링)**:
  - **상태**: UI 껍데기만 존재
  - **결핍**: SNMP 프로토콜 등을 통해 L2/L3 스위치의 대역폭, PoE 전력 사용량, 포트 상태를 실시간으로 폴링(Polling)해오는 백엔드 서비스가 없습니다.
- **`HardwareSelfHealingShell.tsx` (하드웨어 자가 치유)**:
  - **상태**: UI 껍데기만 존재
  - **결핍**: 엣지 NPU/GPU의 온도, 메모리 누수를 감지하여 백엔드가 SSH로 원격 재부팅을 때리거나 컨테이너를 재시작하는 스크립트 실행 엔진이 없습니다.

## 3. 데이터 분석 및 프라이버시 (Data & Privacy)
- **`PrivacyExportWorkshop.tsx` (프라이버시 마스킹 반출)**:
  - **상태**: UI 껍데기만 존재
  - **결핍**: 경찰이나 외부에 영상을 반출할 때, 영상 내 모든 사람의 얼굴이나 차량 번호판을 모자이크 처리(Blurring)하기 위한 백엔드 비디오 트랜스코딩 렌더링 파이프라인(FFmpeg + AI 검출)이 없습니다.
- **`RealtimeBiDashboard.tsx` (실시간 비즈니스 인텔리전스)**:
  - **상태**: UI 껍데기만 존재
  - **결핍**: 매장 내 히트맵(Heatmap), 피플 카운팅 등 장기 통계 데이터를 Aggregation(집계)하는 백엔드 스케줄러와 DB 쿼리 최적화가 안 되어 있습니다.

## 4. 외부 인터페이스 및 유틸리티
- **`MobilePatrolApp.tsx` (모바일 순찰 앱)**, **`MassDeviceConfigClone.tsx` (일괄 설정 복제)**:
  - **상태**: 프론트엔드 모듈만 작성됨
  - **결핍**: 모바일 푸시 알림 연동(FCM/APNs), GPS 좌표 추적 웹소켓 연동, 다중 기기 환경설정 파일(JSON/XML) 일괄 푸시 배치 서버 미구현.

---
### 요약 및 향후 방향
현재 시스템의 척추(Core Pipeline)는 완성되었으나, 팔다리(Advanced Features)에 해당하는 **12개 이상의 심화 컴포넌트들**이 백엔드와의 통신이나 핵심 로직(AI 튜닝, 하드웨어 제어, 영상 렌더링)을 기다리고 있습니다. 

위 미구현 기능들 중 **가장 먼저 실제 동작 수준으로 끌어올리고 싶은 우선순위 도메인**이 있으신지(예: PTZ 카메라 제어, Semantic 영상 검색, 마스킹 반출 등) 말씀해 주시면, 해당 영역을 **Phase 5**로 지정하여 집중적으로 개발해 드리겠습니다.
