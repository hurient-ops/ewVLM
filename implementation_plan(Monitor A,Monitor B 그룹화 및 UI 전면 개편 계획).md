# Monitor A / Monitor B 그룹화 및 UI 전면 개편 계획 (수정본)

사용자님의 상세한 피드백을 모두 반영하여, 누락된 UI 요소를 복구하고 여백 문제를 해결하며, 사용자 편의성을 극대화하기 위해 사이드바 메뉴를 대폭 간소화하는 방향으로 계획을 전면 수정하였습니다.

## User Review Required
> [!IMPORTANT]
> 아래 명시된 3가지 개선 사항에 대한 세부 계획을 확인해 주세요. 
> 특히 **사이드바 메뉴 간소화(핵심 메뉴만 남기고 나머지는 화면 내에서 이동)** 설계가 마음에 드신다면 **[Proceed(진행)]** 버튼을 눌러주시면 즉시 코드 전면 수정 작업에 착수하겠습니다.

## Proposed Changes

### 1. Monitor A (실시간 관제) - 누락된 기능 복구 및 여백 제거
#### [MODIFY] `frontend/src/components/MonitorALiveControl.tsx`
- **PTZ 제어 컨트롤러 추가**: 기획서 명세에 맞추어 화면 하단(또는 측면)에 팬/틸트(Pan/Tilt) 조그셔틀, Zoom In/Out, Focus 조절 아이콘 및 슬라이더를 명시적으로 추가합니다.
- **다중 화면 분할 옵션 확장**: 현재 1분할, 4분할만 있는 레이아웃 선택기를 기획안대로 `1, 2, 4, 8, 16, 32분할`로 확장하여 렌더링합니다.
- **상단 검은 여백(Black Space) 제거**: 4분할 영상 위쪽에 위치한 불필요한 빈 박스(누락된 헤더나 빈 여백) CSS를 제거하고, 비디오 뷰어 영역이 상단부터 꽉 차도록 레이아웃 그리드를 수정합니다.

### 2. 컨텐츠 영역 Full-Width 꽉 차게 수정 (전역 레이아웃)
#### [MODIFY] `frontend/src/components/*.tsx` (CameraSecurityPortal 등 다수 화면)
- 기존 화면들에 부여되어 있던 `max-w-7xl`, `mx-auto` 등 중앙 정렬 및 가로폭 제한 클래스 속성을 일괄 제거합니다.
- `flex-1`, `w-full`, `h-full` 속성을 강제 주입하여, 왼쪽 사이드바 바로 옆부터 브라우저 우측 끝까지 빈 공간 없이 UI가 꽉 차게(Full Width/Height) 랜더링되도록 수정합니다.

### 3. 네비게이션(GNB) 구조 개편 및 사이드바 메뉴 대폭 간소화
#### [MODIFY] `frontend/src/components/GlobalLayout.tsx` 및 `App.tsx`
- **최상단 화면 전환 탭 배치**: 레이아웃 맨 위에 `[Monitor A (VMS 인프라)]` 와 `[Monitor B (VLM 영상분석)]` 대형 탭 버튼을 고정 배치하여 언제든 직관적으로 화면 그룹을 넘나들 수 있게 합니다.
- **사이드바 메뉴 최소화 (종속 화면 숨김)**: 25개에 달하던 사이드바 메뉴를 대폭 줄여 핵심 메뉴(그룹당 3~4개)만 노출합니다.
  - **Monitor A 핵심 사이드바**: `실시간 관제(Monitor A)`, `저장영상/시맨틱 검색`, `GIS 스마트 맵`, `이벤트 리뷰`
  - **Monitor B 핵심 사이드바**: `VLM 분석(Monitor B)`, `재난대응 가상 워룸`, `통계/BI 대시보드`
- **화면 내 종속 이동(In-page Routing) 적용**: 나머지 18개의 세부/설정 화면(예: 카메라 설정, NVR 스토리지, 보안 포탈, 네트워크 토폴로지 등)은 사이드바에서 완전히 제거합니다. 대신, `Monitor A` 나 특정 화면 안의 상단 `[⚙️ 환경설정]`, `[네트워크 보기]` 등의 버튼을 통해서만 진입하도록 라우팅 링크(`useNavigate`)를 연결하여 사용자 동선을 깔끔하게 정리합니다.

## Verification Plan

### Manual Verification
1. `npm run dev` 실행 후 최상단 탭을 통해 Monitor A와 Monitor B가 정상 전환되는지 확인합니다.
2. 좌측 사이드바 메뉴가 핵심 항목 몇 개만 깔끔하게 노출되는지 브라우저 에이전트로 캡처합니다.
3. `MonitorALiveControl` 화면에서 상단 빈 공간이 사라지고, PTZ 컨트롤러와 다중 분할 아이콘(1~32)이 잘 노출되는지 확인합니다.
4. `CameraSecurityPortal` 등 설정 화면 진입 시 중앙에 몰려있던 UI가 왼쪽부터 우측까지 화면에 꽉 차게 렌더링되는지 점검합니다.
