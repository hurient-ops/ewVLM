# 브이월드(VWorld) 폐쇄망 오프라인 지도 적용 분석 및 구현 계획

본 문서는 ewVLM 프로젝트가 인터넷이 차단된 **폐쇄망(인트라넷)** 환경에서 운영될 때를 대비하여, 국토교통부 브이월드(VWorld) 및 국가 공간정보 데이터를 활용한 로컬 지도 서버 구축 방안을 분석하고 프론트엔드 적용 계획을 정리한 것입니다.

---

## 1. VWorld 오프라인 지도 데이터 확보 방법 (분석)

브이월드 Open API는 인터넷망을 통해서만 서비스되므로 폐쇄망 내부에서 직접 호출이 불가능하며, 타일 이미지를 임의로 크롤링(무단 다운로드)하여 사용하는 것은 약관 위반 소지가 있습니다. 따라서 **국가 공식 창구를 통한 오프라인 데이터 수령 및 로컬 서버 구축**이 필수적입니다.

### 방법 A: 브이월드(VWorld) 인트라넷 패키지 신청 (공공기관/지자체 대상)
- **개요:** 공간정보산업진흥원(또는 관련 기관)에서 공공기관 및 업무망(폐쇄망)을 위해 제공하는 오프라인 지도 패키지입니다.
- **절차:** 
  1. 발주처(또는 수요기관) 명의로 공간정보산업진흥원에 데이터 제공 요청 공문 발송
  2. 승인 후 외장하드 등을 통해 오프라인으로 2D 지도 타일, 위성 정사영상 등의 데이터 수령
- **특징:** 배경지도 타일 형태(TMS 등)로 구축되어 제공되는 경우가 많아 로컬에 띄우기 수월합니다.

### 방법 B: 국토정보플랫폼을 통한 원시 데이터 직접 다운로드 (일반 기업 대상)
- **개요:** 국토지리정보원이 운영하는 **[국토정보플랫폼 바로e맵](https://map.ngii.go.kr/)**에서 전국 공간정보(수치지도, 항공사진 등)를 무상으로 다운로드하는 방식입니다.
- **절차:** 
  1. 회원가입 후 '공간정보' 메뉴에서 필요한 지역의 지도 범위 선택
  2. 온라인 직접 다운로드 (또는 용량이 클 경우 오프라인 외장하드 신청 후 방문/우편 수령)
- **특징:** `.shp` (수치지도) 파일 등 원시 데이터 형태로 제공되므로, 이를 시각적 지도로 변환해주는 지도 서버(GeoServer 등) 엔진 구축 기술이 반드시 수반되어야 합니다.

---

## 2. 프론트엔드 시스템 적용 계획 (Architecture)

확보한 오프라인 지도를 ewVLM 프로젝트에 연동하기 위한 아키텍처 변경 사항입니다.

### [Phase 1] 로컬 GIS 타일 서버 구축 (백엔드 / 인프라)
- **도입 기술:** `GeoServer` (오픈소스 공간정보 서버)
- **적용:** 수령한 오프라인 VWorld 지도 타일 데이터나 국토정보플랫폼의 SHP 파일들을 폐쇄망 내부 GeoServer에 업로드합니다.
- **결과:** 카카오맵 API를 대체할 사내 로컬 지도 서비스 API(WMTS/WMS) Endpoint가 생성됩니다. (예: `http://localhost:8080/geoserver/wms`)

### [Phase 2] 프론트엔드 지도 엔진 교체 (React 연동)
현재 카카오맵 SDK에 종속되어 있는 모니터링 뷰의 지도 컴포넌트를 표준 오픈소스 지도 라이브러리로 교체합니다.

#### [NEW] 라이브러리 설치
- `leaflet`, `react-leaflet` : 외부 API 의존성 없이 자체 타일 서버와 연결할 수 있는 전 세계 1위 오픈소스 웹 지도 라이브러리입니다.

#### [MODIFY] 지도 뷰어 컴포넌트 교체 (`HardwareSelfHealingShell.tsx` 등)
- 카카오맵(`react-kakao-maps-sdk`) 렌더링 부분을 걷어내고 `MapContainer`, `TileLayer`를 사용합니다.
- URL 패턴을 로컬 망에 구축된 GeoServer 주소로 바라보도록 변경합니다.

```tsx
// 교체 예정인 Leaflet 기반 코드 예시
import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';
import 'leaflet/dist/leaflet.css';

export default function OfflineMapDashboard() {
  return (
    <MapContainer center={[37.3595, 127.1054]} zoom={15} style={{ height: "100%", width: "100%" }}>
      {/* 카카오맵 대신 자체 구축한 로컬 GeoServer의 주소를 입력 (망 내 URL) */}
      <TileLayer
        url="http://[내부망_서버_IP]:8080/geoserver/gwc/service/wmts/..." 
        attribution="&copy; 국토지리정보원"
      />
      
      {/* CCTV 마커 표출 부분 */}
      <Marker position={[37.3595, 127.1054]}>
        <Popup>외곽 1구역 펜스 북부 (CAM-01)</Popup>
      </Marker>
    </MapContainer>
  );
}
```

> [!WARNING]
> **오프라인 지도의 용량 이슈**
> 브이월드 전국 고해상도 위성 지도 타일을 모두 오프라인으로 받으면 수 테라바이트(TB) 급 용량이 필요합니다. 따라서 타겟 관제 현장(특정 시, 도, 혹은 공장 부지)으로 범위를 좁혀 데이터를 수령 및 구축하는 것이 권장됩니다.

---

## User Review Required
위의 분석 내용과 적용 방안(GeoServer + Leaflet.js 오픈소스 조합)을 확인해 주시기 바랍니다. 

**향후 폐쇄망 납품을 위해 당장 카카오맵을 제거하고 Leaflet 기반의 지도(현재는 임시로 OpenStreetMap 무료 타일 사용)로 프론트엔드 구조를 선제적으로 변경하는 작업을 바로 진행할까요?** 승인해 주시면 코드 변경 작업을 시작하겠습니다.
