# ewVLM 고도화 전략 로드맵 (Phase 9 ~ 11)

앞서 제안드린 5가지 갭 분석 항목을 바탕으로, 비즈니스 파급력과 기술적 의존성을 고려해 **가장 효율적인 개발 순서와 방법(How-to)** 을 세 단계(Phase 9~11)로 재구성했습니다.

---

## 🎯 Phase 9: 통합 관제 및 실시간 현장 통신 (Incident & Dispatch)
**가장 시급한 "실시간 위협 대응 체계"를 완성하는 단계입니다.** 웹 상황실과 모바일 기기가 실시간으로 연결됩니다.

1. **개발 내용**:
   - **통합 알람 센터 (Web)**: VLM이 탐지한 모든 이벤트를 우선순위별 큐(Queue)로 관리하는 `AlertCenterDashboard.tsx` 신규 개발.
   - **상태 관리 체계**: 알람의 상태(미확인 → 확인됨 → 해결됨/에스컬레이션)를 변경할 수 있는 백엔드 API 연동.
   - **모바일 디스패치 연동 (Mobile)**: 현재 비어있는 `MobilePatrolApp.tsx`에 WebSocket(또는 SSE) 클라이언트를 붙여, 웹 관제실에서 할당한 임무를 모바일 화면에 실시간 Push 알림으로 띄우는 기능 구현.
2. **어떻게 (How)**:
   - FastAPI 백엔드에 `websockets` 라이브러리를 활용한 `/ws/alerts` 실시간 통신 엔드포인트 신설.
   - 프론트엔드에서는 `Zustand`를 활용해 전역 알람 상태를 관리하고 실시간 UI 업데이트 처리.

## 🎯 Phase 10: 포렌식 타임라인 및 감사 추적 (Forensics & Audit)
**과거의 사건을 법적 증거로 활용하고, 시스템의 무결성을 증명하는 단계입니다.**

1. **개발 내용**:
   - **다중 타임라인 플레이어**: 과거 영상을 되감고 줌인/줌아웃 할 수 있는 `ForensicTimelinePlayer.tsx` 구현. 타임라인 바 위에 VLM 이벤트 마커(빨간 점 등) 표시.
   - **증거 영상 내보내기**: 특정 타임라인 구간 지정 후, 메타데이터(시간, 장소, VLM 탐지 결과)가 박힌 영상 다운로드 로직 추가.
   - **시스템 감사 로그 (Audit Trail)**: 사용자의 모든 액션(로그인, 카메라 조작, 영상 내보내기)을 기록하고 열람하는 `AuditLogViewer.tsx` 패널 개발.
2. **어떻게 (How)**:
   - 백엔드에 과거 녹화 영상 메타데이터와 이벤트 인덱스를 결합하여 반환하는 `GET /api/v1/records/timeline` API 개발.
   - FastAPI Middleware를 추가하여 모든 상태 변경 API 호출을 가로채고(Intercept), 액션 내역을 감사 로그 DB(모의 딕셔너리)에 적재.

## 🎯 Phase 11: 엔터프라이즈 하드웨어 및 보안 제어 (Enterprise Fleet & RBAC)
**대규모 상용 솔루션으로서의 보안 관리 및 엣지 유지보수 기능을 완성합니다.**

1. **개발 내용**:
   - **펌웨어 배포 센터 (OTA)**: 수십 대의 엣지 장비 펌웨어를 롤링(순차적) 업데이트하고 성공률을 차트로 보는 `EdgeOTAUpdater.tsx` 개발.
   - **장비 헬스케어 차트**: 온도, 디스크 수명, GPU 점유율 트렌드를 시계열 그래프(Chart.js 또는 Recharts)로 가시화.
   - **역할 기반 접근 제어 (RBAC)**: '일반 관제사'는 설정 메뉴에 접근하지 못하게 하고, '최고 관리자'만 펌웨어 업데이트를 할 수 있도록 UI 렌더링 분기 처리.
2. **어떻게 (How)**:
   - 프론트엔드 컴포넌트들에 `useAuth` 훅을 연동하여, 유저 Role이 'Admin'인지 'Viewer'인지 판단해 컴포넌트 렌더링을 차단(Conditional Rendering).
   - 백엔드의 펌웨어 배포 API에서 비동기 프로그레스바(Progress) 시뮬레이션을 위한 Chunk 단위 상태 반환 로직 구현.

---

### 👉 진행 제안
가장 핵심적이고 시각적 체감이 큰 **Phase 9 (알람 센터 및 모바일 WebSocket 연동)** 부터 시작하는 것을 강력히 추천합니다. 
진행을 원하시면 **"Phase 9 시작해줘"** 라고 말씀해 주세요!
