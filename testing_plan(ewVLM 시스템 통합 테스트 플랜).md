# ewVLM 시스템 통합 테스트 플랜

현재 모든 프론트엔드 모듈(23개)이 성공적으로 개발되었으며, 개발 서버(Frontend & Backend)가 동작 중입니다. 
본격적인 기능 점검과 품질 보증(QA)을 위해 다음의 4단계 테스트 방향을 제안합니다.

## 단계 1: UI / UX 및 네비게이션 시나리오 테스트
가장 기본적인 라우팅과 화면 렌더링, 레이아웃 동작을 확인합니다.

* **메인 네비게이션 확인**: 
  - 상단 탭(`실시간 영상`, `저장영상`, `대시보드`, `VLM 분석`) 간의 전환이 빠른 속도(SPA)로 이루어지는지 테스트.
* **환경설정 및 제어 메뉴**: 
  - 우측 상단의 톱니바퀴 아이콘을 클릭하여 드롭다운이 정상적으로 열리는지 확인.
  - `운영 및 제어`(모바일 순찰, IP 오디오 등) 및 `시스템 설정`(토폴로지, 다중 기기 설정 등)의 모든 하위 메뉴로 이동해 렌더링 확인.
* **새 창 분리 (Detached Mode) 기능**:
  - `VLM 분석` 도메인(Monitor B) 진입 후 우측 상단의 **새 창으로 분리** 버튼 클릭.
  - 팝업이 차단되지 않고 열리는지, 듀얼 모니터 환경처럼 메인 창과 분리된 창의 상태가 연동(BroadcastChannel)되는지 확인.

## 단계 2: 핵심 상태 관리(Zustand) 및 컴포넌트 인터랙션 테스트
개별 컴포넌트에 구현된 로컬 상태와 글로벌 상태 관리 로직을 검증합니다.

* **대시보드 GIS 스마트 맵 (`GisSmartMap`)**: 
  - 카카오맵이 기본 줌 레벨(13)로 렌더링되는지 확인.
  - 드래그 앤 드롭, 우측 하단의 PTZ 컨트롤 조그셔틀 패널 조작 테스트.
* **모바일 순찰 뷰 (`MobilePatrolApp`)**: 
  - 대형 **무전 전송(PTT)** 버튼을 길게 클릭(마우스 다운) / 터치했을 때 시각적 피드백(버튼이 빨간색으로 변경되며 '전송 중...')이 제대로 동작하는지 테스트.
* **프라이버시 반출 워크샵 (`PrivacyExportWorkshop`)**: 
  - 영상 반출 버튼 클릭 시 로딩 스피너 및 비활성화 상태가 2.5초간 유지되는지 확인.
* **네트워크 토폴로지 (`NetworkTopologyMonitor`)**:
  - 활성화된 토폴로지 노드(스위치/카메라) 클릭 시 우측의 상세 인스펙터 패널이 토글(열림/닫힘)되는지 확인.
* **카메라 슬롯 드래그 앤 드롭 (`MonitorALiveControl`)**: 
  - 좌측 자산 탐색기에서 카메라를 드래그하여 중앙의 빈 영상 슬롯에 드롭할 때 카메라가 바인딩되는지 테스트.

## 단계 3: 모의 이벤트(Event Simulator) 연동 및 알람 테스트
실시간 관제 시스템의 핵심인 알림 및 이벤트 파이프라인을 테스트합니다.

* **자동 이벤트 발생기 (Simulator)**:
  - `App.tsx` 또는 `useEventSimulator`를 활성화하여 15초 단위로 모의 VLM 이벤트(침입 감지, 화재 등)를 발생시킵니다.
* **글로벌 알림 전파**: 
  - 이벤트 발생 시 우측 상단 알림 아이콘에 뱃지가 표시되는지 확인.
  - GIS 맵에 해당 카메라 위치의 마커가 경고 색상(Red) 및 애니메이션으로 깜빡이는지 점검.
  - 하단의 공통 알림 스낵바(Toast) 또는 시스템 이벤트 로그가 즉시 업데이트되는지 확인.

## 단계 4: 백엔드 API & WebSocket 연결 테스트
FastAPI 서버와의 통신 안정성을 확인합니다.

* **API 엔드포인트 연동**:
  - `http://localhost:8000/api/v1/cameras`에서 카메라 기초 데이터를 잘 가져오는지 네트워크 탭 확인.
* **WebRTC 영상 스트림**: 
  - 각 슬롯의 `WebRTCPlayer`가 백엔드 미디어 서버(`http://localhost:8889`)로부터 더미 비디오 피드 연결을 시도하는지 확인.

---

> [!TIP]
> **추천 진행 방식**
> 위 4가지 단계 중 **단계 1(UI/네비게이션)**과 **단계 2(인터랙션)**를 먼저 제가 하나씩 가이드해 드리며 함께 테스트해보는 것을 추천합니다. 동의하시면 즉시 첫 번째 시나리오를 시작해 볼 수 있습니다!
